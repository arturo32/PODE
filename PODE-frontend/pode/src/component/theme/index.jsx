import { createTheme } from '@mui/material/styles';

export const form = createTheme({
    palette: {
        primary: {
            main: '#66C4D9 ',
        },
    },
});

export const generic = createTheme({
    palette: {
        primary: {
            main: '#F25C05 ',
        },
    },
});